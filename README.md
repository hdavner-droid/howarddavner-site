# howarddavner.com

Source for https://howarddavner.com — static site, auto-deployed to Netlify on every commit to `main`.

- Articles live in `insights/`
- `sitemap.xml` + `insights.html` list all articles
